variable app_name {
    description = "The environment to deploy the resources to"
    default     = "entraid_web_auth_reference"
    type        = string
}

variable env {
    description = "The environment to deploy the resources to"
    default     = "dev"
    type        = string
}

variable location {
    description = "The environment to deploy the resources to"
    default     = "West Europe"
    type        = string
}