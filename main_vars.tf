variable env {
    description = "The environment to deploy the resources to"
    default     = "dev"
    type        = string
}

variable app_name {
    description = "The environment to deploy the resources to"
    default     = "entra_auth_reference"
    type        = string
}

variable location {
    description = "The environment to deploy the resources to"
    default     = "West Europe"
    type        = string
}